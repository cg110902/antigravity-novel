# 后续测试方向与注意事项

> 本文供新会话接手继续测试使用。截至当前：BUG#1~#47 已修复，回归 152 项全通过，
> 实战两本书 check 均 0 errors，PR #2 已提交至 main。

---

## 一、必读的前置上下文

| 文件 | 作用 |
|---|---|
| `BUGS.md` | **权威缺陷清册**（47 条，含现象/根因/修复/验证）。接手第一件事是通读，避免重复挖已修的洞。 |
| `COVERAGE.md` | 逻辑覆盖清册：26 顶层命令 / 42 组合、G1~G10 硬阻断、check 7 节、探针清单、sync 14 表顺序。 |
| `tests/regression_test.py` | 152 项端到端回归。**任何修改后必跑**：`python3 tests/regression_test.py`（cwd 必须是项目根）。 |
| `engine/README.md` | 7 条设计不变量。判断"是 bug 还是设计"时以此为准。 |

### 环境
- Python 3.11，无三方依赖；版本常量 `4.3.2`（`engine/__init__.py`，**已落后于实际的 4.3.3 修复，可考虑同步**）
- 实战工作区：`workspace/lantern`（古代悬疑，双卷 16 章）、`workspace/testbook`（都市异能，2 章）

---

## 二、尚未验证的功能点（按优先级）

### P0 — 从未测过，风险最高

1. **`cruise` 长程无人值守的真实长度**
   - 现状：回归里只测到 10 章 + 跨卷 6 章。
   - 建议：用 `tests/_cruise_fixture.py` 生成 **50~100 章**跑一次，观察：
     - 台账是否随章数线性膨胀（`timeline.json` / `co_occurrence.json` 尤其可疑）
     - `state rollup` 的折叠是否真能抑制膨胀
     - 内存与耗时曲线
   - 命令：`python3 tests/_cruise_fixture.py /tmp/long 60 vol_01 1 && python3 studio.py cruise --once -w /tmp/long`

2. **多卷（≥3 卷）的 rollup 累积行为**
   - 现状：只测到 2 卷。`rollup_vol_01.json` / `rollup_vol_02.json` 并存时，
     cockpit / reconcile / export 是否正确合并？有无重复计数？
   - 重点看 `export` 的总字数与各卷 rollup 之和是否自洽（注意 BUG#35 的口径差异）。

3. **并发/中断安全**
   - sync 写 14 张表，中途 `Ctrl-C` 或磁盘满会怎样？是否有半写状态？
   - 测法：`timeout 0.05 python3 studio.py sync ch_XXX -w <ws>` 反复触发，之后跑 `check` 看台账是否损坏。
   - 引擎宣称"原子封存"，这条**必须验证**。

### P1 — 部分验证，仍有死角

4. **`evolution`（Stage 4C 剧情反转）全链路**
   - `simulate impact` 已测（正常），但**真正执行反转**（如让已死角色复活、推翻 locked_facts）从未走通。
   - 这是唯一能合法修改 `locked_facts` 的路径，值得重点测。

5. **`config set/get` 对各探针的实际影响**
   - 本轮新增了 `dialogue_ratio` 配置项。需验证 `config set dialogue_ratio "[10,90]"` 后探针是否真的改变行为。
   - 同时检查还有哪些**声明了但未被消费**的配置项（用 BUG#42 的方法论：AST 扫描 + grep 消费点）。

6. **`style` / `profiler` / `dehydrator` 相关的语义质检**
   - 这三块目前**完全没有代码守护**（手册说"语义交由 Stage 3A"）。
   - 可考虑：是否有更多"手册规定了、引擎没守护"的可量化规则？（BUG#43 就是这么挖到的）

7. **`id trace` / `trace` 对已删除实体的行为**
   - 实体被手工从台账删除后，trace 会报错还是静默返回空？

### P2 — 稳健性与边界

8. **超长/超短输入**：单章 10 万字、章节 ID 为 `ch_9999`、人物名含 emoji 或特殊字符
9. **编码边界**：BOM、CRLF、全角标点混排、繁体中文
10. **`parser.py` 自研 mini-YAML 的健壮性**
    - 这是 365 行自研解析器，是**整个系统的单点依赖**。
    - 建议专项 fuzz：嵌套列表、多行字符串、注释中含 `:`、值含 `#`、缩进不一致等。
    - 与标准 YAML 行为的差异点应文档化。

---

## 三、已知未修的遗留问题

| 编号 | 级别 | 问题 | 未修原因 |
|---|---|---|---|
| BUG#9 | — | （见 BUGS.md，记录不改） | 已确认为设计行为 |
| BUG#35 | P3 | export 与 sync 字数口径差异（差值=标题行字数） | 不影响正确性，需先定义"总字数"的官方口径 |
| BUG#45 | P3 | 审计涌现死亡以中文名而非 `p_XXX` 作主键 | 涉及 14 表写入顺序与存量迁移，风险>收益 |

---

## 四、方法论沉淀（这些"套路"挖到过真 bug，建议复用）

1. **扫描"参数收而不用"** → 挖到 BUG#41、#42
   ```bash
   python3 /tmp/scan_unused.py   # 见 BUGS.md BUG#42 条目，AST 遍历比对形参与函数体引用
   ```
   同理可扫：返回值被丢弃的调用、只写不读的字段、定义后无消费者的常量。

2. **手册断言 vs 代码实现逐条比对** → 挖到 BUG#43
   - 提取手册中所有**可确定性量化**的规则（数字、阈值、顺序、枚举），逐条找对应代码。
   - 找不到代码的，就是"规定了没人守护"。

3. **新校验反哺存量数据** → 挖到 BUG#46
   - 每加一条一致性校验，**立刻对所有历史工作区跑一遍全书 check**，常能考古出旧账错误。

4. **判断"是 bug 还是设计"的标准**：看**误判后果**
   - 影响硬裁决（阻断、入账、删除）→ 必须确定性，不能靠启发式；
   - 只影响提醒 → 启发式可接受，不完备不是缺陷。
   - 这条标准直接催生了 BUG#47 的分层重构。

5. **写入侧宽容、消费侧无感**：本项目最高发的 bug 族（#21/#24/#25/#28/#30/#40/#44/#46）
   - 排查手法：找所有 `if X not in db:` / `try: ... except: pass` / 静默 `continue`，
     问一句"这里跳过时，用户会知道吗？"

---

## 五、操作注意事项（踩过的坑，务必遵守）

1. **破坏性测试一律在 `/tmp` 隔离副本进行**
   - 本轮 BUG#36 的 PoC 直接在正本工作区跑，**导致 8 章未提交内容被删除**。
   - 正确做法：`cp -r workspace/lantern /tmp/poc && ...  -w /tmp/poc`

2. **每轮 commit 前先 `git log --oneline -3` 确认父提交**
   - 工作区可能是全新 clone，本地历史会从旧基线开始，直接 commit 会导致 push 被拒。
   - rebase 解冲突后必须逐项校验：BUGS.md 条目数 / 代码修复标记 / 回归数 / 两本书 check。
   - `git rebase --continue` 需带 `GIT_EDITOR=true`。

3. **修改已 finalize 的正文必须同时改 `raw/` 与 `final/` 两份**

4. **`grep -c` 统计 check 告警会把「💡 方案」行算进去**，实际条数用 `grep -A1` 目视确认

5. **测试残留必须清理**：G10 类阻断测试会留下细纲/正文，全局 check 会持续报错；
   需同时删 `outlines/vol_XX/beats/ch_XXX.md` 与 `manuscript/*/raw|final` 对应文件。

6. **`edit_file` 写中文注释时极易误输入 `//`** 导致 Python 语法错误——本轮发生过两次。

7. **地点字段契约**：环境法定字段是 `sensory_anchor` + `environment_rules`，
   自造 `atmosphere` 不被 check 3.3 认可。

---

## 六、我的建议（优先级排序）

1. **先做第二节的 P0-3（中断安全）**——"原子封存"是引擎的核心承诺，
   但从未被验证过。如果这里有问题，性质比已修的任何一个 bug 都严重。

2. **其次做 P2-10（parser fuzz）**——365 行自研 YAML 解析器是全系统单点，
   一旦解析出错，所有下游校验都建立在错误数据上。

3. **考虑把 `engine/__init__.py` 的版本常量从 4.3.2 提到 4.3.3**，
   与本轮修复对齐；`check` 有版本一致性巡检，长期不同步会让该巡检失去意义。

4. **可以考虑引入 `--strict` 模式**：把当前所有 warning 升级为 error。
   给追求严谨的作者一个选项，也便于回归测试暴露更多问题。

5. **长期方向**：当前所有语义判断都是正则/词表。若要根本性提升，
   可考虑在 L2 层引入轻量语义模型（但**必须守住 BUG#47 的分层边界**——
   L1 永远只认结构化契约，绝不让模型输出直接驱动硬裁决）。

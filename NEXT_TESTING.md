# 后续测试方向与注意事项

> 本文供新会话接手继续测试使用。截至当前：BUG#1~#47 已修复，回归 152 项全通过，
> 实战两本书 check 均 0 errors，PR #2 已提交至 main。

---

## 一、必读的前置上下文

| 文件 | 作用 |
|---|---|
| `BUGS.md` | **权威缺陷清册**（47 条，含现象/根因/修复/验证）。接手第一件事是通读，避免重复挖已修的洞。 |
| `COVERAGE.md` | 逻辑覆盖清册：26 顶层命令 / 42 组合、G1~G10 硬阻断、check 7 节、探针清单、sync 14 表顺序。 |
| `tests/regression_test.py` | 152 项端到端回归。**任何修改后必跑**：`python3 tests/regression_test.py`（cwd 必须是项目根）。 |
| `engine/README.md` | 7 条设计不变量。判断"是 bug 还是设计"时以此为准。 |

### 环境
- Python 3.11，无三方依赖；版本常量 `4.3.2`（`engine/__init__.py`，**已落后于实际的 4.3.3 修复，可考虑同步**）
- 实战工作区：`workspace/lantern`（古代悬疑，双卷 16 章）、`workspace/testbook`（都市异能，2 章）

---

## 二、尚未验证的功能点（按优先级）

### P0 — 从未测过，风险最高

1. **`cruise` 长程无人值守的真实长度**
   - 现状：回归里只测到 10 章 + 跨卷 6 章。
   - 建议：用 `tests/_cruise_fixture.py` 生成 **50~100 章**跑一次，观察：
     - 台账是否随章数线性膨胀（`timeline.json` / `co_occurrence.json` 尤其可疑）
     - `state rollup` 的折叠是否真能抑制膨胀
     - 内存与耗时曲线
   - 命令：`python3 tests/_cruise_fixture.py /tmp/long 60 vol_01 1 && python3 studio.py cruise --once -w /tmp/long`

2. **多卷（≥3 卷）的 rollup 累积行为**
   - 现状：只测到 2 卷。`rollup_vol_01.json` / `rollup_vol_02.json` 并存时，
     cockpit / reconcile / export 是否正确合并？有无重复计数？
   - 重点看 `export` 的总字数与各卷 rollup 之和是否自洽（注意 BUG#35 的口径差异）。

3. **并发/中断安全**
   - sync 写 14 张表，中途 `Ctrl-C` 或磁盘满会怎样？是否有半写状态？
   - 测法：`timeout 0.05 python3 studio.py sync ch_XXX -w <ws>` 反复触发，之后跑 `check` 看台账是否损坏。
   - 引擎宣称"原子封存"，这条**必须验证**。

### P1 — 部分验证，仍有死角

4. **`evolution`（Stage 4C 剧情反转）全链路**
   - `simulate impact` 已测（正常），但**真正执行反转**（如让已死角色复活、推翻 locked_facts）从未走通。
   - 这是唯一能合法修改 `locked_facts` 的路径，值得重点测。

5. **`config set/get` 对各探针的实际影响**
   - 本轮新增了 `dialogue_ratio` 配置项。需验证 `config set dialogue_ratio "[10,90]"` 后探针是否真的改变行为。
   - 同时检查还有哪些**声明了但未被消费**的配置项（用 BUG#42 的方法论：AST 扫描 + grep 消费点）。

6. **`style` / `profiler` / `dehydrator` 相关的语义质检**
   - 这三块目前**完全没有代码守护**（手册说"语义交由 Stage 3A"）。
   - 可考虑：是否有更多"手册规定了、引擎没守护"的可量化规则？（BUG#43 就是这么挖到的）

7. **`id trace` / `trace` 对已删除实体的行为**
   - 实体被手工从台账删除后，trace 会报错还是静默返回空？

### P2 — 稳健性与边界

8. **超长/超短输入**：单章 10 万字、章节 ID 为 `ch_9999`、人物名含 emoji 或特殊字符
9. **编码边界**：BOM、CRLF、全角标点混排、繁体中文
10. **`parser.py` 自研 mini-YAML 的健壮性**
    - 这是 365 行自研解析器，是**整个系统的单点依赖**。
    - 建议专项 fuzz：嵌套列表、多行字符串、注释中含 `:`、值含 `#`、缩进不一致等。
    - 与标准 YAML 行为的差异点应文档化。

---

## 三、已知未修的遗留问题

| 编号 | 级别 | 问题 | 未修原因 |
|---|---|---|---|
| BUG#9 | — | （见 BUGS.md，记录不改） | 已确认为设计行为 |
| BUG#35 | P3 | export 与 sync 字数口径差异（差值=标题行字数） | 不影响正确性，需先定义"总字数"的官方口径 |
| BUG#45 | P3 | 审计涌现死亡以中文名而非 `p_XXX` 作主键 | 涉及 14 表写入顺序与存量迁移，风险>收益 |

---

## 四、方法论沉淀（这些"套路"挖到过真 bug，建议复用）

1. **扫描"参数收而不用"** → 挖到 BUG#41、#42
   ```bash
   python3 /tmp/scan_unused.py   # 见 BUGS.md BUG#42 条目，AST 遍历比对形参与函数体引用
   ```
   同理可扫：返回值被丢弃的调用、只写不读的字段、定义后无消费者的常量。

2. **手册断言 vs 代码实现逐条比对** → 挖到 BUG#43
   - 提取手册中所有**可确定性量化**的规则（数字、阈值、顺序、枚举），逐条找对应代码。
   - 找不到代码的，就是"规定了没人守护"。

3. **新校验反哺存量数据** → 挖到 BUG#46
   - 每加一条一致性校验，**立刻对所有历史工作区跑一遍全书 check**，常能考古出旧账错误。

4. **判断"是 bug 还是设计"的标准**：看**误判后果**
   - 影响硬裁决（阻断、入账、删除）→ 必须确定性，不能靠启发式；
   - 只影响提醒 → 启发式可接受，不完备不是缺陷。
   - 这条标准直接催生了 BUG#47 的分层重构。

5. **写入侧宽容、消费侧无感**：本项目最高发的 bug 族（#21/#24/#25/#28/#30/#40/#44/#46）
   - 排查手法：找所有 `if X not in db:` / `try: ... except: pass` / 静默 `continue`，
     问一句"这里跳过时，用户会知道吗？"

---

## 五、操作注意事项（踩过的坑，务必遵守）

1. **破坏性测试一律在 `/tmp` 隔离副本进行**
   - 本轮 BUG#36 的 PoC 直接在正本工作区跑，**导致 8 章未提交内容被删除**。
   - 正确做法：`cp -r workspace/lantern /tmp/poc && ...  -w /tmp/poc`

2. **每轮 commit 前先 `git log --oneline -3` 确认父提交**
   - 工作区可能是全新 clone，本地历史会从旧基线开始，直接 commit 会导致 push 被拒。
   - rebase 解冲突后必须逐项校验：BUGS.md 条目数 / 代码修复标记 / 回归数 / 两本书 check。
   - `git rebase --continue` 需带 `GIT_EDITOR=true`。

3. **修改已 finalize 的正文必须同时改 `raw/` 与 `final/` 两份**

4. **`grep -c` 统计 check 告警会把「💡 方案」行算进去**，实际条数用 `grep -A1` 目视确认

5. **测试残留必须清理**：G10 类阻断测试会留下细纲/正文，全局 check 会持续报错；
   需同时删 `outlines/vol_XX/beats/ch_XXX.md` 与 `manuscript/*/raw|final` 对应文件。

6. **`edit_file` 写中文注释时极易误输入 `//`** 导致 Python 语法错误——本轮发生过两次。

7. **地点字段契约**：环境法定字段是 `sensory_anchor` + `environment_rules`，
   自造 `atmosphere` 不被 check 3.3 认可。

---

## 六、我的建议（优先级排序）

1. **先做第二节的 P0-3（中断安全）**——"原子封存"是引擎的核心承诺，
   但从未被验证过。如果这里有问题，性质比已修的任何一个 bug 都严重。

2. **其次做 P2-10（parser fuzz）**——365 行自研 YAML 解析器是全系统单点，
   一旦解析出错，所有下游校验都建立在错误数据上。

3. **考虑把 `engine/__init__.py` 的版本常量从 4.3.2 提到 4.3.3**，
   与本轮修复对齐；`check` 有版本一致性巡检，长期不同步会让该巡检失去意义。

4. **可以考虑引入 `--strict` 模式**：把当前所有 warning 升级为 error。
   给追求严谨的作者一个选项，也便于回归测试暴露更多问题。

5. **长期方向**：当前所有语义判断都是正则/词表。若要根本性提升，
   可考虑在 L2 层引入轻量语义模型（但**必须守住 BUG#47 的分层边界**——
   L1 永远只认结构化契约，绝不让模型输出直接驱动硬裁决）。



=============



# 一、下一轮应该做什么

我原本把「sync 中断安全」列为最高优先级，刚才**已经实测完毕**，现在可以给出确定结论，这改变了下一轮的优先级排序：

### 实测结论：原子性部分成立，但有一处真实缺口

**成立的部分（可放心）**
- `_save_json` 用 `tempfile.mkstemp` + `fsync` + `os.replace`，**单文件原子性真实有效**。中断后无 `.tmp` 残留，无 JSON 半截损坏。
- 中断后补跑 `sync`，台账与「一次干净跑完」**逐字节完全一致**。资金池 `银两 47 / 灵石 -10`、transactions 列表均无重复扣减 —— **幂等设计真实有效**。
- `check` 能检出半写状态：报出「章节已定稿但未合账……该章的台账变更很可能整章遗失」并给出补救命令。

**缺口（这是真问题）**
> **sync 是 14 次独立的单文件原子写，不是一个跨表事务。**

`_save_json` 有 20 个调用点，中断在第 5 次时，persons/items/places/factions 已落盘，后 9 张表没有 —— 台账处于**跨表不一致**状态。救命的是 `sync_log` 恰好在最后写，所以幂等重放能兜住。但这是**顺序上的巧合，不是机制上的保证**：任何人日后调整落盘顺序、或在 sync_log 之后再加一次写表，这道防线就静默失效。而且中间态窗口里，其它命令（cockpit / export / proposal）读到的是不一致台账。

**所以下一轮第一件事，不是再测中断，而是把这个"巧合"变成"契约"。**

### 下一轮优先级（修订版）

| 序 | 事项 | 理由 |
|---|---|---|
| **1** | **给 sync 加跨表事务保护 + 落盘顺序回归锁** | 缺口已实证。修复成本低、收益高 |
| **2** | **`parser.py` fuzz** | 365 行自研 YAML 是全系统单点，**从未被专项测过** |
| **3** | 版本常量 4.3.2 → 4.3.3 | 已证实不一致，check 有版本巡检，顺手修掉 |
| 4 | 长程 60~100 章压测 | 台账膨胀与 rollup 折叠有效性 |
| 5 | `evolution` 真正执行反转 | 唯一能合法改 `locked_facts` 的路径，从未走通 |

另外顺带发现一个**新问题**：fixture 生成的 ch_017 声明 `it_001` 为新实体「长明灯」，但台账里 `it_001` 已是「豁口铜灯签」。check 正确报出撞号，但 lantern 里还有 `p_001` 在台账登记为「陆青」、细纲写「裴砚」的 **id↔name 错配存量脏数据**（ch_014/015/016 各报一次）。这是 BUG#37 修复后**考古出的存量错账**，值得单独清理。

---

# 二、详细测试方案

## 方案 A：sync 跨表事务保护（P0，建议首做）

### A1. 先锁住现状，防止回归
落盘顺序目前是「隐性契约」，需变成显式断言：

```python
# tests/regression_test.py 新增
def test_sync_log_is_last_write():
    """sync_log 必须是最后一次落盘 —— 中断幂等性的唯一依赖。"""
    order = []
    orig = S._save_json
    S._save_json = lambda p, d: (order.append(p.name), orig(p, d))[1]
    try:
        O.sync_chapter(ws, 'ch_XXX', force=True)
    finally:
        S._save_json = orig
    assert order[-1] == 'sync_log.json', f"落盘顺序被破坏：最后一次是 {order[-1]}"
```

### A2. 中断点全覆盖扫描
不要只测一个中断点，要**遍历所有 N 个落盘点**：

```python
for k in range(1, 21):          # 在第 k 次落盘时中断
    setup_clean_workspace()
    interrupt_at(k)
    resync()                     # 补跑
    assert_ledger_equals(clean_baseline)   # 与干净基线逐字节比对
```

这是**最高价值的一个测试**。20 个中断点全过，才能说幂等性成立；只要有一个点补跑后台账与基线不同，就是真 bug。重点盯有「增量扣减」语义的表：`ledger`（资金）、`items`（充能）、`debts`（恩怨发号）。

### A3. 中间态可见性
中断后、补跑前，执行 `cockpit` / `export` / `check`，确认它们**要么正确报错，要么不输出误导性数据**。目前 `check` 表现合格，其余命令未验证。

### A4. 建议的修复方向（三选一，按成本排序）
1. **最小成本**：写一个 `state/.sync_pending` 哨兵文件，sync 开始时创建、结束时删除。任何命令启动时若发现它存在，直接硬失败提示「上次 sync 未完成，请重跑 `sync <ch> --force`」。
2. **中等成本**：收敛 20 个 `_save_json` 到一个 `_commit_all(tables: dict)`，先全部写 `.tmp`，再统一 `os.replace`。窗口从「14 次写盘」缩到「14 次 rename」，量级收窄。
3. **彻底**：写前置 journal，记录本次要改的全部表与旧值，中断后可回滚。成本最高，对本项目可能过度设计。

我倾向 **1 + A1 组合**：哨兵挡住误用，回归锁住顺序，投入产出比最高。

---

## 方案 B：parser.py fuzz（P1）

365 行自研 mini-YAML，6 个解析函数，**全系统所有配置与细纲都经它入口**。一旦解析出错，所有下游校验都建立在错误数据上——这是比任何单个 bug 都危险的位置。

### B1. 差分测试（核心手法）
最有效的办法是**与标准 YAML 对拍**。沙箱无三方依赖，但可用 Python 内置手工构造期望值，或临时安装 PyYAML 只用于测试对拍：

```python
cases = [...]
for text in cases:
    mine = parse_mini_yaml(text)
    ref  = yaml.safe_load(text)
    if mine != ref:
        record_divergence(text, mine, ref)   # 差异不一定是 bug，但必须解释清楚
```
**所有差异点都要有明确结论**：是有意简化（写进文档），还是真 bug。

### B2. 重点构造用例（按风险排序）

| 风险点 | 用例 |
|---|---|
| 注释剥离 | 值里含 `#`：`name: 灯#签`、`url: "a#b"`、`# 整行注释`、行尾 ` # 注释` |
| 引号处理 | `_strip_quotes` 对 `"a'b"`、`'a"b'`、`""`、单个 `"` |
| 键值切分 | `_split_key_value` 遇 `时间: 12:30`、`比例: 3:7`、`a: b: c` |
| 标量推断 | `_parse_scalar` 对 `007`、`1e5`、`true/True/TRUE`、`null/~`、`-`、`0x1F`、`+86` |
| 中文特有 | 全角冒号 `：`、全角引号 `「」""`、繁体、emoji、零宽字符 |
| 结构 | 缩进不一致、tab 混空格、空列表 `[]`、嵌套列表、多行字符串 `|` 与 `>` |
| 边界 | 空输入、只有 `---`、超长行（10万字符）、BOM、CRLF |

### B3. 往返一致性（property-based）
`dump_mini_yaml` 与 `parse_mini_yaml` 应互逆：

```python
assert parse_mini_yaml(dump_mini_yaml(data)) == data
```
随机生成嵌套 dict/list/中文字符串跑 10000 轮。**这条最容易抓到真 bug**——尤其 `_yaml_str` / `_yaml_key` 的转义是否覆盖了 `_strip_quotes` 的全部输入空间。

### B4. 恶意输入
超深嵌套（1000 层）是否栈溢出？超长单行是否卡死？解析失败时是抛异常还是静默返回部分结果？**静默部分成功是最危险的**——参考 BUG 族「写入侧宽容、消费侧无感」。

---

## 方案 C：存量脏数据清理（P2）

lantern 的 `p_001` 在台账为「陆青」、细纲为「裴砚」，ch_014/015/016 各报一次。

1. `python3 studio.py id list person -w workspace/lantern` 确认权威值
2. 判断是台账错还是细纲错（看 `entity_timeline` 首次登场记录）
3. 批量修正后全书 `check` 必须回到 0 errors 且无 warning
4. **同时排查 testbook 是否有同类问题**

这类「新校验反哺存量数据」的清理，本轮已验证是高产手法（BUG#46 就是这么挖到的）。

---

## 方案 D：长程压测（P2）

```bash
python3 tests/_cruise_fixture.py /tmp/long 60 vol_01 1
time python3 studio.py cruise --once -w /tmp/long
```

观测四项：
- `timeline.json` / `co_occurrence.json` 体积随章数增长曲线（怀疑**平方级膨胀**——共现是 N² 关系）
- `state rollup` 折叠后是否真能压下体积
- 单章 sync 耗时是否随已有章数上升（若上升说明每次全量重算）
- 峰值内存

**判据**：若 co_occurrence 确认平方增长，300 章长篇会不可用——这是架构级问题，需尽早发现。

---

## 执行建议

**下一轮开场三件事，按顺序**：
1. 跑 **A2 中断点全覆盖**（20 个点），这是本轮实证的自然延伸，最可能立刻抓到真 bug
2. 加 **A1 落盘顺序回归锁** + A4 方案 1 哨兵
3. 开始 **B3 往返一致性** fuzz

顺手做掉版本常量 4.3.2 → 4.3.3。

所有内容我已整理进 `NEXT_TESTING.md`，需要我现在就把本轮的中断安全实证结论和这份修订后的方案写进去吗？

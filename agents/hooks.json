{
  "novel-studio-guard": {
    "enabled": true,
    "PostToolUse": [
      {
        "matcher": "write_to_file",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys; sys.path.insert(0,'tools'); from verify_double_ledgers import verify_ledgers; r=verify_ledgers(as_json=True); sys.exit(1 if (isinstance(r,dict) and r.get('anomalies')) else 0)\"",
            "timeout": 20
          }
        ]
      },
      {
        "matcher": "replace_file_content",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys; sys.path.insert(0,'tools'); from verify_double_ledgers import verify_ledgers; r=verify_ledgers(as_json=True); sys.exit(1 if (isinstance(r,dict) and r.get('anomalies')) else 0)\"",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
